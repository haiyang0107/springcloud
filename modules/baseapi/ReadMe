baseapi 作用是为其余服务提供统一的接口规范，以及 api 调用。
需要注意：在相关的 client 上需要填写好 服务的名称以及对应值

同时由于 feign 采用 http 内容请求方式，根据restful 风格 获取 服务数据，

所以需要定义好 相应的请求方式以及数据类型（接口，非引用数据类型 容器类的需要注意在传输过程中的数据丢失问题，因为通过反射机制获取数据，但是容器无法进行实例化，所以获取不到数据）